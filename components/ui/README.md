# ui/

Small shared primitives (buttons, dividers, section labels) used across
`intro/`, `tunnel/`, and `portfolio/`. Empty until Stage 3+ needs a
component in more than one place — avoid premature abstraction.
